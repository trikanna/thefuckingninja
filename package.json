{
  "name": "thefuckingninja",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch -p 8181",
    "test": "eslint src && preact test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^4.1.1",
    "if-env": "^1.0.0",
    "preact-cli": "^1.3.0"
  },
  "dependencies": {
    "preact": "^8.1.0",
    "preact-compat": "^3.16.0",
    "preact-router": "^2.5.4",
    "styled-components": "^2.1.1"
  }
}
